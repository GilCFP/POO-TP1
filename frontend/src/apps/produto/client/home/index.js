import React from 'react';
import ReactDOM from 'react-dom';
import ProdutoApp from './ProdutoApp';

const container = document.getElementById('root');
const root = ReactDOM.createRoot(container);
root.render(<ProdutoApp />);
