# Media Files Directory

This directory is used to store user-uploaded media files (images, documents, etc.) for the Django application.

Files uploaded through the Django admin or API will be stored here during development.