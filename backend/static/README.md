# Static Files Directory

This directory is used to store static files for the Django application during development.

During production, use `python manage.py collectstatic` to collect all static files here.